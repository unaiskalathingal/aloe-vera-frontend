# netlify.toml

# Build settings
[build]
  command = "CI= npm run build"
  publish = "build"
     

# Redirects for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
